Ayudame a completar para crear un proyecto que al ejecturarlo me pregunte el nombre del proyecto le asigne un numero segun el que sigue en su base de datos y el ponga ese numero y el nombnre del proyecto. agregale opciones como config db donde consulta la base de datos los numeros por donde va cotizacion y hace la carpeta con ese numero - nombre_proyecto. entonces si no le pongo base de datos lo hace con el nombre del proyecto. la url la guarda en la carpeta de configuracion en ~/.config/orgmorg/config.json y la utliza si existe para consultar. la carpeta template tiene las carpteas cradas que se van a crear. en la configurracion tambien debe debe tener config path ,, que es el path donde creara los proyectos de lo contrario se crean en el lugar de ejecucion de los proyectos. tambien quiero un comando que se llame organize donde hace una carpta por cada tipo de archivo que encuentre en la carpeta y lo organiza segun su extension. rar, exe, xlsx, y asi sucesivamente. tambien quiero otro comando organize-by-date donde organiza los archivos por fecha en la que se crearon en la pc., no la fecha  de creacion del archivo.

la base de ddatos se debe popular con una tabla con los id e cotizacion y otra tabla con los proyectos id, nombre